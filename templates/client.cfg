# File managed by Puppet

# Derived from sample configuration for send_nsca(8).
# See the send_nsca.cfg(5) man page.
#
# PLEASE SET THE PERMISSIONS OF THIS FILE APPROPRIATELY TO MAKE SURE THAT ONLY
# AUTHORIZED USERS CAN ACCESS IT!
#

server      = "<%= @server %>"
password    = "<%= @password %>"
identity    = "<%= @identity %>"
tls_ciphers = "<%= @tls_ciphers.join(':') %>"
port        = "<%= @port %>"
timeout     = "<%= @timeout %>"
delay       = "<%= @delay %>"
